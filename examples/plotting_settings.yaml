robot: "trunk"
save_dir: "plots/"
file_format: "png"

show: {
    'ssmr_origin': False,
    'ssmr_nnssm': False,
    'ssmr_nnssm_fail1': True,
    'tpwl': False,
    'koopman': False,
    'rompc': False
}
display_name: {
    'ssmr_origin': "SSMR (origin)",
    'ssmr_nnssm': "NNSSM (n=9)",
    'ssmr_nnssm_fail1': "NNSSM (fail 1)",
    'tpwl': "TPWL",
    'koopman': "Koopman",
    'rompc': "ROMPC",
}
color: {
    'ssmr_origin': 'tab:blue',
    'ssmr_nnssm': 'tab:orange',
    'ssmr_nnssm_fail1': 'tab:red',
    'tpwl': 'tab:red',
    'koopman': 'tab:green',
    'rompc': 'tab:purple',
    'target': 'black'
}
linestyle: {
    'ssmr_origin': '-',
    'ssmr_nnssm': '-',
    'ssmr_nnssm_fail1': '-',
    'tpwl': '-',
    'koopman': '-',
    'rompc': '-',
    'target': '--'
}
linewidth: {
    'ssmr_origin': 2,
    'ssmr_nnssm': 2,
    'ssmr_nnssm_fail1': 2,
    'tpwl': 2,
    'koopman': 2,
    'rompc': 2,
    'target': 1
}

select_target: "figure8"
# Figure8 target
define_targets: {
    figure8: {
        M: 1,
        T: 10,
        N: 1000,
        radius: 40,
        # state constraints
        z_lb: null, # [-16, -16],
        z_ub: null  # [16, 5]
    },
    circle: {
        M: 1,
        T: 10,
        N: 1000,
        radius: 30,
        z_const: -10,
        # state constraints
        z_lb: null, # [-16, -16],
        z_ub: null  # [16, 5]
    }
}

plot_mpc_rollouts: False


# TODO
##############################################
# Problem 2, Circle on side
##############################################
# M = 3
# T = 5
# N = 1000
# t_target = np.linspace(0, M*T, M*N)
# th = np.linspace(0, M*2*np.pi, M*N)
#
# r = 15
# phi = 17
# x_target = np.zeros(M*N)
# y_target = r * np.sin(phi * T / (2 * np.pi) * th)
# z_target = r - r * np.cos(phi * T / (2 * np.pi) * th) + 107.0

# NAME = 'circle'